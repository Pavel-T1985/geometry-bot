services:
  - type: background
    name: geometry-bot
    plan: free
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: VK_TOKEN
        sync: false
      - key: GIGACHAT_KEY
        sync: false
